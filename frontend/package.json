{
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && tsup",
    "build-watch": "tsup --watch src/**/*.ts"
  },
  "devDependencies": {
    "@types/bootstrap": "^5.2.10",
    "axios": "^1.7.9",
    "mermaid": "^11.4.1",
    "tsup": "^8.3.6"
  }
}
